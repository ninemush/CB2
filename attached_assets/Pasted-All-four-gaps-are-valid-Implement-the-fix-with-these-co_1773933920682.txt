All four gaps are valid. Implement the 
fix with these corrections:

PASS 1 — Match complete elements, not 
just opening tags

The regex must match the full element 
including attributes and closing syntax.

For self-closing tags:
  /<(ui:)?(?:TODO_|PLACEHOLDER_)\w+\b[^>]*?\/>/g

For open-close tag pairs (including 
children):
  /<(ui:)?(?:TODO_|PLACEHOLDER_)(\w+)\b[^>]*?>[\s\S]*?<\/\1?(?:TODO_|PLACEHOLDER_)\2>/g

Run both replacements. Each match is 
replaced with:
  <ui:Comment Text="REVIEW: Unknown 
  activity type was generated here — 
  implement manually" />

This handles the full element including 
any children, preventing orphaned 
closing tags from corrupting the XML.

PASS 2 — Strip remaining tokens from 
attribute values and text content

After Pass 1 has removed all tag-name 
occurrences, run both replacements:
  .replace(/TODO_\w*/g, '')
  .replace(/PLACEHOLDER_\w*/g, '')

These are now safe because all tag-name 
occurrences were handled in Pass 1.

Fix line 1137 too — it has the identical 
PLACEHOLDER_ bug:
  .replace(/PLACEHOLDER_\w*/g, '')

Both lines 1137 and 1138 are fixed by 
the two-pass approach.

LOG ELEVATION

Elevate BOTH debug log calls to 
console.warn:
- Line 4705 (validation failure log)
- Line 4713 (catch block log)

COMPLETION CRITERIA

1. Generate WeatherAndTravelNotification 
   — zero XML parse errors

2. If any activity type contained a 
   TODO_ or PLACEHOLDER_ token, confirm 
   it appears as a ui:Comment in the 
   generated XAML rather than a 
   malformed tag

3. Confirm "Invalid space after '<'" 
   does not recur

4. All existing tests still pass

5. New tests:
   - Self-closing <ui:TODO_HttpClient 
     .../> is converted to a ui:Comment, 
     not to <ui: .../>
   - Open-close pair <TODO_Activity 
     ...>children</TODO_Activity> is 
     converted to a single ui:Comment, 
     not left with orphaned tags
   - PLACEHOLDER_ token in a tag name 
     is handled identically to TODO_

6. Both console.warn calls appear in 
   server logs when wellformedness 
   validation fails — verify by 
   checking logs on the next failure