Both errors need upstream fixes, not 
patches for specific cases. Here is 
the correct approach for each.

ERROR 2 — Duplicate DisplayName (upstream fix)

Add "DisplayName" to PSEUDO_XAML_ATTR_KEYS 
in xaml-generator.ts line 1773.

This prevents the generator from ever 
emitting DisplayName as a property 
attribute. The explicit DisplayName= 
on the element tag remains as the single 
source. This fixes the duplicate for 
every activity type, not just the one 
that triggered this run.

Also add a duplicate-attribute stripper 
to the post-processing pipeline in 
uipath-integration.ts as a safety net. 
For each XAML file, parse the XML and 
check every element for repeated 
attribute names. Keep the first occurrence, 
remove subsequent duplicates. This is 
a generic fix — it catches any duplicate 
attribute regardless of which key is 
repeated.

ERROR 1 — Unescaped ampersand (fix the 
source, not just the sanitiser)

The post-processing sanitiser is a safety 
net but it has a coverage gap — content 
assembled or mutated after the initial 
xamlEntries build may not pass through it.

The upstream fix is to ensure escapeXml() 
is called at the point of interpolation, 
not in a post-processing pass. 

In xaml-generator.ts, find every location 
where a string value from the LLM output 
or from user-derived content is 
interpolated into an XML attribute value 
or text content. Specifically:

1. Any place where a VB expression string 
   is placed inside an attribute value — 
   ensure it passes through escapeXml() 
   before being written into the template

2. Any place where display strings, 
   message content, or annotation text 
   from the LLM are interpolated — ensure 
   escapeXml() wraps the value

3. The email body composition in 
   particular — HTML content with 
   ampersands in CSS or URLs will 
   consistently fail if not escaped 
   at the point of interpolation

After fixing the interpolation points, 
keep the post-processing sanitiser as 
a final safety net but do not rely on 
it as the primary fix.

Show me the locations in xaml-generator.ts 
where LLM string values are interpolated 
into XAML without escapeXml() before 
implementing the fix. I want to confirm 
the scope before you make changes.

Do not fix only the specific line that 
caused this failure. Fix the pattern 
across all interpolation points.