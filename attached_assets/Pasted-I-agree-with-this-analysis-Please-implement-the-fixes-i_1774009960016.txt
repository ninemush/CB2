I agree with this analysis. Please implement the fixes in this order, and do not broaden scope beyond these items yet.

Priority 1 — Fix stale status overwrite
Problem:
At the end of generateUiPath(), the success handler can overwrite a valid backend status like FALLBACK_READY or READY_WITH_WARNINGS with READY because it uses a stale closure value of uipathBuildStatus.

What I want:
- Make backend-emitted status authoritative.
- Do not let the end-of-stream success handler overwrite FALLBACK_READY or READY_WITH_WARNINGS.
- Use a ref or another safe mechanism if needed, but the simplest acceptable fix is to stop doing a stale conditional overwrite.
- Preserve FAILED if already set.

Priority 2 — Unify cache-hit response behavior
Problem:
The /api/ideas/:ideaId/generate-uipath endpoint returns plain JSON for cache hits and SSE for fresh generations. The frontend does not populate warnings/templateComplianceScore/outcomeSummary consistently in the JSON path.

What I want:
- Make the endpoint behave through one canonical response shape.
- Preferred: return a minimal SSE stream even on cache hits so the frontend has one code path.
- If you strongly believe JSON must remain, then update the frontend to fully hydrate all UiPath status metadata from that JSON path as well.
- In either case, the UI must preserve:
  - status
  - warnings
  - templateComplianceScore
  - outcomeSummary

Priority 3 — Only if needed after 1 and 2
- Review the UiPath trigger handoff and reduce unnecessary complexity only if the first two fixes reveal more orchestration issues.
- Do not refactor the whole workspace state machine in this pass unless required.

Important constraints:
- Preserve the current public CB2 repo architecture.
- Do not change the backend package-generation logic itself.
- Do not touch the survivability pipeline contract unless required by the cache-hit unification.
- Keep CannonBall’s UX honest: degraded builds must remain visibly degraded.

Before coding, give me a short implementation note:
1. how you will make status authoritative
2. whether you will unify cache hits via SSE or frontend JSON hydration
3. which files you will edit first

After implementation, report back with:
- what changed
- why FALLBACK_READY / READY_WITH_WARNINGS can no longer be lost
- how cache-hit generations now surface full metadata
- anything I should test immediately afterward
