I agree with this evaluation. Please turn the upstream hardening plan into a concrete implementation spec and task breakdown.

We should treat this as a five-pillar fix:

1. UiPath Studio Compatibility Profile
2. UiPath Activity/Package Source of Truth
3. Deterministic dependency selection
4. Pre-emission WorkflowSpec validation
5. Structural-preservation remediation

Important framing:
This is an upstream package-fidelity hardening effort.
The goal is not just “Studio opens the package.”
The goal is:
- dependency-correct package generation
- activity/property-correct XAML generation
- less destructive remediation
- preservation of executable workflow structure

Please incorporate these conclusions explicitly:

A. Keep the Studio Compatibility Profile lightweight
- Do not model all of Studio
- A small curated compatibility object is enough:
  - studioLine
  - targetFramework
  - projectType
  - allowed package version ranges
  - minimum package lines where needed

B. Make the Activity/Package Source of Truth curated, not a live NuGet scraper
- Extend the current activity catalog into a stronger executable contract
- Add:
  - enum value constraints
  - version compatibility
  - feed availability / approved version info
- Prioritize the highest-value correctness items first:
  - package versions
  - enum values
  - required properties

C. Move primary validation earlier
- Validate WorkflowSpec against the source of truth before XAML assembly
- Keep the post-emission quality gate as a safety net
- Do not rely on late quality-gate failures to discover basic activity/property incompatibilities

D. Fix structural remediation
- Add a structure-preserving remediation mode
- Never let Level 3 erase meaningful control-flow structure if the workflow skeleton is still salvageable
- Main.xaml must be especially protected
- Preserve:
  - Sequence
  - If/Else
  - Try/Catch
  - loops
  - invoke relationships
- Stub only failing leaves/properties where possible

E. Address process-type classification risk
- The current process classification is too blunt to aggressively narrow the activity set
- Either improve classification materially or expose a broader validated catalog slice to the LLM
- Prefer broader valid choice set + stronger downstream validation over over-pruning too early
- Also account for prompt token budget by defining:
  - compact prompt format
  - full validation format

What I want back:
1. A concrete implementation spec
2. A phased task breakdown in recommended implementation order
3. For each phase:
   - objective
   - done looks like
   - out of scope
   - tasks
   - relevant files
   - dependencies
4. Explicit acceptance criteria for:
   - dependency correctness
   - activity/property correctness
   - Main.xaml structure preservation
   - reduced destructive stubbing
   - Studio compatibility alignment

Recommended implementation order:
1. Studio Compatibility Profile
2. Activity/Package Source of Truth
3. Deterministic dependency selection
4. Pre-emission WorkflowSpec validation
5. Structural-preservation remediation

Important:
- Preserve the existing good architecture:
  - two-phase pipeline
  - tree-based workflow assembly
  - quality gate
- This should harden inputs and remediation behavior, not replace the whole builder
- Be concrete and implementation-oriented, not abstract
