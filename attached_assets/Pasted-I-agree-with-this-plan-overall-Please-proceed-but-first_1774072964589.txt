I agree with this plan overall. Please proceed, but first incorporate the following implementation cautions and metadata requirements into the design.

I want this combined into one updated implementation prompt.

Core direction to keep:
- unified metadata freshness architecture
- covers both generation metadata and integration metadata
- refresh happens out of band
- runtime uses local deterministic last-known-good snapshots
- runtime adoption must replace scattered hardcoded assumptions

Required refinements:

1. Do NOT derive the UiPath Studio target/LTS line purely from latest package major versions.
- That is too heuristic.
- The Studio target in generation-metadata.json must be an explicit curated field.
- Package metadata can be refreshed automatically, but Studio target selection must come from a more principled rule/source than package-major inference.

2. Separate endpoint confidence from endpoint reachability.
- service-endpoints.json must carry curated confidence metadata:
  - official
  - inferred
  - deprecated
- The refresh probe must separately record runtime accessibility/health:
  - reachable
  - limited
  - unreachable
- A successful probe must NOT automatically upgrade an inferred endpoint into an official one.

3. Make refresh partial-safe.
- If generation metadata refresh succeeds but integration metadata refresh fails, update only generation metadata and preserve the previous last-known-good integration snapshot.
- If integration metadata refresh succeeds but generation metadata refresh fails, do the reverse.
- Do not require all-or-nothing refresh when the snapshot families are independent.

4. Define snapshot reload behavior clearly.
- Do not let call sites read files ad hoc.
- Use a centralized loader/service with in-memory state.
- On successful refresh, explicitly reload the in-memory snapshot once.
- Runtime consumers should read from the in-memory metadata service, not directly from files.

5. Clarify ownership between activity-catalog.json and generation-metadata.json.
- activity-catalog.json should own:
  - activity/property schema
  - enum constraints
  - package mapping
- generation-metadata.json should own:
  - Studio target
  - package version ranges
  - preferred versions
  - freshness metadata
- Avoid duplicating version truth across both files long term.

6. Add verification timestamps everywhere relevant.
I want timestamp fields so we know when each version and endpoint was last verified.

For generation metadata:
- snapshot-level fields:
  - lastRefreshedAt
  - lastVerifiedAt
  - sourceVersion
  - snapshotVersion
- package-level fields where relevant:
  - lastVerifiedAt
  - verificationSource
- if helpful, package version range entries can also carry:
  - lastVerifiedAt

For integration metadata:
- snapshot-level fields:
  - lastRefreshedAt
  - lastVerifiedAt
  - snapshotVersion
- per-endpoint fields:
  - lastVerifiedAt
  - verificationSource
  - confidence
  - reachabilityStatus
  - deprecationNotes
- If probe history is tracked, keep at least the latest verification timestamp.

7. Surface freshness clearly.
- On startup:
  - warn if snapshot age > 7 days
  - error if snapshot age > 30 days
- In admin UI:
  - show current Studio target
  - last refreshed
  - last verified
  - staleness status
  - package count
  - endpoint count
  - manual Refresh Now button

Important:
- Do NOT solve this with manual JSON edits as the primary method
- Do NOT add live-network latency to package generation
- Do NOT leave the metadata unused after refresh
- Keep package generation deterministic and low-latency
- Keep integration endpoint handling current without scattering guessed strings across the codebase

What I want back:
1. revised implementation notes for #136 incorporating all of the above
2. updated snapshot schema proposal
3. confirmation of how timestamps will be stored and surfaced
4. confirmation of how runtime adoption will work through centralized in-memory metadata services

Then proceed with implementation planning using the revised design.
