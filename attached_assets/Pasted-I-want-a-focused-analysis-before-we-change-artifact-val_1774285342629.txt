I want a focused analysis before we change artifact validation behavior.

Context
The current SDD artifact-block flow appears to have become slower and more failure-prone after the stricter validation and extended retry chain were added.

The key question is not simply “should validation be stricter or looser?”
The key question is:

What is the current artifact validation actually protecting downstream, and which parts of it are now creating unnecessary failure and latency?

What I want analyzed

1. Downstream dependency mapping
Map every downstream consumer of `orchestrator_artifacts`, including:
- package generation
- provisioning
- deployment manifest creation
- any UI/status/readiness logic
For each consumer, identify exactly which artifact fields are actually required for correct behavior.

2. Validation rule inventory
List the current validation rules in `validateArtifactBlock()` / related logic.
For each rule, classify it as:
- parse-level validity
- minimum deployment usability
- field completeness / quality
- strict schema conformance

3. Hard-fail vs warning analysis
For each current validation rule, determine:
- if this rule fails, does downstream truly break?
- or could downstream still operate safely with a warning/degraded state?

I want a recommendation for which rules should be:
- hard failures
- warnings
- informational only

4. Retry amplification analysis
Trace the current artifact-generation and recovery chain:
- primary artifacts call
- retries
- ensureArtifactBlock retries
- validation
Show how many LLM calls can happen in the worst case today and which validation failures are triggering repeated retries.

5. Net value analysis
Answer clearly:
- what downstream value the stricter validation is providing today
- whether that value justifies the extra latency/failure risk
- whether the current validation threshold is too strict for the actual consumers

6. Recommendation
Based on the analysis, recommend one of these:
- keep strict validation as-is
- move to tiered validation (hard fail vs warning)
- loosen validation to minimum deployable contract
- reduce retry chain and keep validation
- both tier validation and reduce retries

Deliverable
Please return:
1. downstream consumer map
2. current validation rule inventory
3. hard-fail vs warning recommendation per rule
4. retry-chain analysis
5. concise recommendation on what should change next

Important
- Do not implement the fix yet
- I want to understand whether validation is improving downstream correctness or just creating another failure point
- Ground the answer in the actual code paths, not just theory
