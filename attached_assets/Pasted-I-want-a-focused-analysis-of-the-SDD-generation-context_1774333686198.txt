I want a focused analysis of the SDD generation context to determine whether we are sending too much information, redundant information, or low-value information to the LLM.

Important:
Do NOT implement a fix yet.
I want an evidence-based analysis first.

Context
SDD generation is currently heavy, slow, and fragile:
- it can take 2-5+ minutes
- the artifacts path is especially unreliable
- the prompts appear to include a large amount of platform and integration context
- we need to understand whether all of that context is actually necessary or whether some of it is making the generation slower and less reliable without improving the output

Concrete example:
For Integration Service, the tenant has around 20 active connections, but the SDD context may be including the full 169-connector catalog plus operation metadata. That seems likely to be low-value or actively harmful for generation quality and latency.

What I want analyzed

1. Full SDD prompt-context inventory
Map exactly what data is currently sent into:
- the SDD prose generation prompt
- the SDD artifacts generation prompt
- any retry/extraction prompts

For each prompt, list:
- full chat history or summary
- process map context
- platform capabilities/service inventory
- Integration Service connector catalog
- active connections
- connector operation catalogs
- package version registry
- approvals/status context
- any other major injected context blocks

2. Value assessment by context block
For each context block, classify:
- essential
- useful but compressible
- low-value / likely redundant
- likely harmful (adds token cost/noise without helping output)

I want the reasoning tied to actual output needs, not just intuition.

3. Prose vs artifacts separation
Determine which context is actually needed for:
- prose sections of the SDD
- orchestrator_artifacts generation

I strongly suspect these two prompts should not receive the same context payload.
Please identify:
- what the artifacts prompt truly needs
- what the prose prompt benefits from
- what should be removed from one or both

4. Integration Service context analysis
Specifically analyze the value of:
- full connector catalog
- active connections only
- connector capability flags
- operation catalogs / actions / triggers
- folder/context scoping details

I want a recommendation on whether the SDD should receive:
- only active connections
- active connections + compact capability summary
- or any broader connector catalog at all

5. Prompt-size / latency / reliability relationship
Estimate, based on the code and observed behavior, how much of the SDD latency/risk is likely due to:
- oversized prompt context
- oversized expected outputs
- context irrelevant to the generation task
- duplicated information between prose and artifacts prompts

6. Recommendation
Recommend a prompt-context minimization strategy that preserves output quality.
For each context block, say whether it should be:
- kept as-is
- compressed
- moved to prose only
- moved to artifacts only
- removed entirely

Deliverable
Please return:
1. full context inventory for SDD prose/artifacts/retry prompts
2. value assessment per context block
3. prose-vs-artifacts context split recommendation
4. Integration Service-specific recommendation
5. concise recommendation on what context should be trimmed or restructured next
6. whether this should become one implementation task or two linked tasks

Important constraints
- do not implement yet
- this is an analysis task
- I want to know whether we are over-contextualizing the SDD generation path
- focus on actual usefulness to output quality, not just token count in the abstract
