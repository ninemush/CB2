I want a focused investigation and task-definition pass that addresses BOTH of the current issues we just saw in the same live UiPath package-generation run:

1. package generation still failed
2. the progress UI still did not tell a truthful story during the run

Important:
Do NOT implement yet.
I want the real root-cause analysis and the right task split first.

Context from the latest live run
The run no longer appears to be “dead” or blocked on SSE transport.

What actually happened:
- the backend progressed through spec decomposition successfully
- scaffold generation completed
- 10 workflows were identified
- per-workflow detail generation ran
- 6 workflows were stubbed due to the 5-minute aggregate timeout
- spec merge/validation completed
- the run moved into the build pipeline
- build then failed later with a package-generation/XAML correctness issue

At the same time:
- the UI still felt almost static to the user
- the data was flowing through the SSE stream
- but the progress panel largely collapsed real progress into a generic “AI Generation / Generating UiPath package...” experience

So this is NOT just one problem.
It is two linked problems:
- package generation quality/reliability
- progress rendering/truthfulness

What I want investigated

Part A — Failed package generation
Please trace the actual final package-generation failure in the latest run and identify the real root cause.

I want:
1. the exact failing stage
2. the exact error class
3. whether this is:
- XAML well-formedness
- namespace/prefix correctness
- argument-wrapper correctness
- InvokeWorkflowFile correctness
- dependency authority
- state-machine/tag balancing
- or something else
4. where in the generation/build path the failure was introduced
5. whether the failure should have been prevented earlier by validation or safe downgrade/stubbing

Important:
I do not want a generic “package build failed” summary.
I want the real upstream root cause and the right task definition for fixing it.

Part B — Progress truthfulness
Please analyze why the UI still did not show a truthful live progress story even though the SSE stream and backend stage events were flowing.

Based on the latest run, the likely issue is now:
- render logic / state retention / progress modeling
not
- the observer SSE connection itself

I want a precise wiring/root-cause analysis of:
1. how pipeline events are reaching the frontend
2. how UiPathProgressPanel / progress rendering groups and filters those events
3. why completed spec-generation stages effectively disappear
4. why the user is left with a generic “AI Generation” + spinner during a multi-minute run
5. whether build-phase entries are being hidden, delayed, or visually collapsed

I want the real rendering/root-cause, not just “the data exists.”

What I want back
Please return:
1. a clear investigation summary for both problems
2. whether these should be:
- one combined implementation task
- or two linked tasks
3. the proposed task(s) in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Important constraints
- do not implement yet
- do not treat this as just an SSE transport issue unless the current evidence truly supports that
- do not treat the UI problem as solved just because events exist in logs
- do not treat the package failure as a generic build failure; identify the real upstream correctness issue
- keep the progress task focused on truthful user-visible progress, not cosmetic redesign
- keep the package task focused on preventing the actual failure class that happened in this run

What I care about most
I want the user to be able to:
- watch a truthful progress story during package generation
- and get a package that actually builds successfully

Please ground the analysis in the latest live run behavior, not just prior assumptions.
