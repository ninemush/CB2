I want a focused investigation and task-definition pass for the latest package-generation failure:

UiPath post-archive parity check failed with 1 mismatch(es):
Content mismatch for validated XAML "Main.xaml"

Important:
Do NOT implement yet.
I want the real root cause and the right task definition first.

Context
This does NOT look like the primary generation/modeling failure itself.
It looks like a pipeline finalization/parity failure:
- `Main.xaml` was validated in one form
- a different `Main.xaml` appears to have been written into the final archive
- the post-archive parity check correctly detected that drift

My current hypothesis
The most likely issue is one of these:

1. Late mutation after validation
- some post-validation step is still modifying `Main.xaml`
- examples could include:
  - placeholder/TODO cleanup
  - remediation/fixup
  - namespace/prefix normalization
  - reachability/invocation-graph reconciliation
  - entry-point rewrite
  - any final compliance pass

2. Different sources of truth
- one in-memory copy of `Main.xaml` is validated
- a different copy is later written to the archive
- likely drift between structures like:
  - xamlEntries
  - deferredWrites
  - validated content
  - archive manifest/update content

3. Archive rebuild/update path drift
- a later archive update/rebuild step modifies or replaces `Main.xaml`
- but the parity expectations/hash tracking are not updated accordingly

What I want investigated

1. Exact lifecycle of Main.xaml
Trace the full lifecycle of `Main.xaml` in the package build:
- where it is first generated
- where it is normalized/compliance-processed
- where it is validated
- where its validated hash is recorded
- where it is appended or updated in the archive
- any later steps that can still mutate it

I want the exact path, not a generic summary.

2. Source-of-truth drift
Identify whether `Main.xaml` exists in multiple collections/representations during the build (for example:
- xamlEntries
- deferredWrites
- archive manifest maps
- validated entry sets
- rebuilt zip entries)

If so, identify:
- which copy is treated as authoritative at each stage
- where they diverge
- why the archive received a different version from the validated one

3. Actual root cause
Tell me which of the following this really is:
- late mutation after validation
- multiple sources of truth drifting
- archive rebuild/update bug
- parity checker bug
- or some combination

4. Correct fix shape
I do NOT want a superficial workaround.
I want the correct architectural fix.

For example, if the issue is source-of-truth drift, the fix should likely be:
- one authoritative final XAML content source before archive write
- no mutation after validation
- or re-validation / re-hashing after any late mutation

If the issue is a late rewrite path, I want that path identified explicitly.

What I want back
Please return:
1. investigation summary
2. exact root cause
3. the implementation task needed, in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Important constraints
- do not patch this one package manually
- do not just weaken or remove the parity check
- assume the parity check is doing something useful unless proven otherwise
- the goal is to ensure the validated XAML and archived XAML are the same final artifact
- keep this focused on the Main.xaml parity mismatch/finalization bug
