I want a focused investigation-to-task-definition pass for the current Studio-open failures in the generated UiPath package.

Important:
Do NOT implement yet.
I want the right task split first.

Context
The latest package is not failing primarily because it is shallow or because it has too many warnings.
It is failing because some generated workflows are structurally invalid for Studio.

From direct inspection of the generated files, we have three concrete upstream failure patterns:

1. Main.xaml contains malformed VB/XML expression serialization
- example pattern: expression text contains XML-escaped string literals and malformed operators inside the VB expression itself
- examples seen:
  - `[str_CurrentEventSendStatus = &quot;Sent&quot;]`
  - malformed operator serialization such as `&amp;obj_Gt;`
- this is causing Studio parse/load failure around Main.xaml line 314

2. Main.xaml contains invalid activity typing / loop wiring
- `ForEach.Values` typed as `InArgument(x:String)` while bound to a DataTable / rows collection
- variables typed as `x:Object` where the workflow later treats them as `x:String` or `x:Int32`
- post-loop assignments reference `obj_CurrentEventRow` while the loop delegate variable is `item`
- these are structural generation defects, not just placeholder business logic gaps

3. InitAllSettings.xaml is being marked “Fully Generated” but is still structurally invalid
- `ForEach.Values` typed as `InArgument(x:String)` while bound to `dt_Settings.Rows` / `dt_Constants.Rows`
- `ExcelReadRange.DataTable` generic typing / namespace usage looks inconsistent
- variable names such as `dt_Constants.Rows` / `dt_Settings.Rows` are not safe workflow variable identifiers
- the DHG and quality reporting are overstating package quality

What I want
Please create multiple linked implementation tasks that address these issues upstream and generically, not as one-off fixes for this birthday use case.

These tasks must resolve the pattern classes, not just patch these specific files.

I want the task set in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Task design requirements

Task A — Expression serialization correctness
Create a task focused on fixing the upstream generation/compliance path so UiPath/VB expression content is serialized correctly in XAML.

This should cover patterns like:
- string literals inside expressions
- comparison operators and encoded characters
- expression-bearing attributes/text nodes
- preventing XML escaping from corrupting the VB expression itself

Important:
- this should be treated as a generic expression-serialization correctness problem
- not a one-off fix for `Sent` / `GuardrailFail` only

Task B — Activity argument and variable type generation correctness
Create a task focused on upstream type correctness for generated workflows.

This should cover patterns like:
- correct `ForEach.Values` typing for collections / DataTables / Rows
- correct `InArgument<>` / `OutArgument<>` generic types
- variable type inference and declaration correctness
- safe variable naming rules
- making sure loop-body variables and later references stay consistent

Important:
- this must be generic for workflow generation, not specific to just Main.xaml or InitAllSettings.xaml
- the goal is to stop generating structurally invalid activity trees

Task C — Quality classification / DHG truthfulness for generated workflows
Create a task to ensure workflows are not marked “Fully Generated” when they still contain structural/catalog/type defects that would prevent clean Studio load or execution.

This should cover:
- tightening the threshold for “fully generated”
- reflecting structural invalidity in the DHG/status
- preventing misleading readiness reporting

Task D — Then decide where #280 belongs
I still think some parts of #280 are useful:
- retry backoff with jitter
- bounded parallelism
- proactive registry-known package resolution
- stronger handling for excessive non-catalog property stripping
- deeper activity-tree prompting

Please determine:
- which parts of #280 should remain as a separate hardening task
- and which parts are blocked behind the more fundamental expression/type correctness fixes above

Important constraints
- do not propose file-specific/manual patching of Main.xaml or InitAllSettings.xaml
- do not treat placeholder injection alone as sufficient
- do not reduce this to just “better prompting”
- fixes must be upstream and reusable for the pattern classes we are seeing:
  - malformed expression serialization
  - wrong activity argument typing
  - unsafe variable naming
  - misleading “fully generated” classification

What I want back
Please return:
1. a short analysis summary of the actual pattern classes
2. the split task set
3. the recommended execution order
4. whether #280 should be delayed, split, or partially retained behind these higher-priority fixes
