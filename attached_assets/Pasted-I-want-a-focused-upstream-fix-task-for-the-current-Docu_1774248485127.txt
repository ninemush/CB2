I want a focused upstream fix task for the current Document Understanding authentication failure.

Important:
Do NOT “fix” this by falling back to Du.Default.
That would push us back toward the same broken path.

Also important:
Before changing the code to hardwire any DU scope form, I want Replit to explicitly test both forms first:
- with `.Api`
- without `.Api`

Do not assume from docs alone which form is accepted in this tenant/runtime.
Prove it first, then implement.

Context
Current evidence shows:
- the tenant/app has real DU scopes configured, including:
  - Du.DocumentManager.Document
  - Du.Digitization.Api
  - Du.Extraction.Api
  - Du.Classification.Api
  - Du.Validation.Api
  - Du.DataDeletion.Api
- CannonBall is still failing DU integration
- Replit identified that pickMinimalFromOidc currently tries to use taxonomy-provided DU scopes even when they do not overlap with OIDC discovery
- OIDC discovery appears incomplete for DU and only surfaces Du.Default
- Falling back to Du.Default is NOT the right fix if the actual tenant/app has the real DU scopes and the API still needs those

What I want
Create a task that fixes DU auth correctly upstream by using a tenant-aware / docs-backed DU scope override strategy, not a blind OIDC fallback.

Goals
- make DU work with the actual DU scopes configured for the tenant/app
- keep the OIDC architecture intact for services where OIDC is accurate
- treat DU as a known OIDC exception if needed
- keep the fix central in MetadataService / auth authority layer
- do not scatter DU hacks across the codebase
- do not regress currently working services

What I want investigated first
Before implementation, verify and report:

1. Exact DU token request contract
Determine which exact DU scope strings UiPath accepts in this tenant for token requests.

You must explicitly test both forms before choosing one:
- request scopes using:
  - Du.Digitization
  - Du.Classification
  - Du.Extraction
  - Du.Validation
- and request scopes using:
  - Du.Digitization.Api
  - Du.Classification.Api
  - Du.Extraction.Api
  - Du.Validation.Api

If needed, also test whether Du.DocumentManager.Document participates in the working DU access path.

Do not assume from docs alone which form is correct.
Use actual runtime evidence.

2. Exact DU JWT grant behavior
After requesting DU scopes, inspect the returned JWT and report:
- which DU scopes were actually granted
- whether they match the requested form or are transformed
- whether Du.DocumentManager.Document or other DU scopes are required for the current projects API

3. Exact failure point
Determine whether the current DU failure is caused by:
- wrong requested scope names
- wrong token family
- missing scopes in the actual token
- missing project-level permission (e.g. Manage Access / Project.Execute)
- wrong endpoint/path
- or some combination

4. Re-evaluate Generative Extraction after DU
Do not assume DU and Generative Extraction are the same problem.
Fix DU first, then re-test Generative Extraction separately.

Implementation requirements
1. Do NOT fallback to Du.Default as the primary DU operational fix
- OIDC may remain incomplete for DU
- Du.Default should not be treated as the authoritative operational DU scope if tenant/runtime evidence shows otherwise

2. Add a DU-specific authority rule centrally
Implement a principled central mechanism in MetadataService/auth scope resolution so DU can use:
- tenant-backed configured scopes
- and/or docs-backed override scopes
ahead of incomplete OIDC discovery

This must:
- live in one central authority layer
- be explicit and traceable
- not require scattered special-cases in probes or UI
- preserve OIDC as the default for other services

3. Prefer actual verified working DU scope form
After testing both `.Api` and non-`.Api` request forms, the implementation must use whichever form is proven to work in the tenant/runtime.
Do not encode the DU scope form in code until the test evidence is collected.

4. Prefer actual configured tenant scopes where available
If the connection/user has saved DU scopes that are valid and present in the tenant/app configuration, prefer those over incomplete OIDC-discovered Du.Default.
The system should not throw away real configured DU scopes just because OIDC is incomplete.

5. Improve DU diagnostics
Add logging/diagnostics that clearly show:
- requested DU scopes
- granted DU scopes
- endpoint used
- response from the DU API
so future DU mismatches are immediately visible

6. Keep everything else safe
Verify no regression to:
- Orchestrator
- Test Manager
- Data Service
- AI Center
- Platform Management
- Maestro
- IXP
- OR deployment/connectivity
- folder loading

What I want back
Please return:

1. concise root-cause summary
2. explicit test results for both scope forms:
- with `.Api`
- without `.Api`
3. exact DU scope request format that UiPath accepts in this tenant
4. exact DU scopes granted in the JWT after the fix
5. design of the central DU override / authority rule
6. files/modules changed
7. whether Document Understanding now works
8. whether Generative Extraction now works after the DU fix
9. if Generative Extraction still fails, exact reason why
10. regression checks performed
11. what I should test immediately

Acceptance criteria
- DU is not “fixed” by blindly falling back to Du.Default
- both `.Api` and non-`.Api` DU scope request forms are tested before implementation chooses one
- DU scope selection uses a central authority rule that can handle incomplete OIDC discovery
- actual tenant-configured/documented DU scopes are respected
- DU token requests use the verified working scope strings for this tenant/runtime
- DU probe succeeds if the tenant/app truly has the required DU scopes and permissions
- currently working services do not regress
- no scattered DU-specific hacks are introduced outside the central authority layer
