I want a focused upstream task-definition pass based on the latest code review findings for UiPath package generation.

Important:
Do NOT implement yet.
I want the real task(s) defined first from the current codebase state.

We are focusing on these three issues:

1. package spec generation is still a single-shot LLM bottleneck
2. progress is still untruthful during the longest initial package-generation phase
3. dependency resolution/version authority is still not reliable enough for Studio-safe output

Grounding findings from code review

Finding A — Single-shot package spec generation bottleneck
In `server/document-routes.ts` (`/api/ideas/:ideaId/generate-uipath`), package generation still:
- builds one large `systemCtx` from full approved SDD
- optionally adds full PDD
- adds serialized process-map summary
- sends one `getCodeLLM().create()` call with `maxTokens: 16384`
- relies mainly on JSON repair after truncation/failure

There is still:
- no workflow-level decomposition
- no per-workflow retry
- no partial-spec persistence
- no bounded local recovery if one workflow/spec segment fails

This confirms the highest-risk all-or-nothing failure is still at the package-spec generation layer.

Finding B — Progress is still untruthful during the longest phase
In `server/document-routes.ts`, while the initial package-spec LLM call is running, the route emits only:
- generic `progress` strings
- rotating keep-alive messages

Structured `pipelineEvent` stage data only starts after JSON parsing and pre-build kickoff.

So the UI still cannot show truthful stage-level progress during the longest and most expensive part of package generation.

Finding C — Dependency authority is still weak
In `server/uipath-integration.ts`, dependency resolution still uses:
- a static `confirmedVersionMap`
- `UNVERIFIED_PACKAGES`
- omission of packages like `UiPath.Web.Activities` from `project.json` even when XAML scanning detects them

This means dependency restore can still drift from what the generated XAML actually references, and package validity still depends partly on hardcoded assumptions rather than a validated source of truth.

What I want back
Please create the next task(s) in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Task design guidance

I expect this may become either:
- two linked tasks
or
- three focused tasks

Please recommend the right split.

What I want the tasks to address

1. Package spec generation decomposition / resilience
I want a task that evaluates and likely introduces a better package-spec generation architecture than the current single massive call.

This should likely address:
- workflow-level or logical-segment decomposition of package spec generation
- smaller/bounded LLM calls instead of one huge call
- per-workflow retry / local recovery
- partial-spec persistence / merge strategy
- preserving shared downstream build pipeline where possible
- avoiding a full second package-generation architecture unless truly necessary

Important:
The task should be grounded in the conclusion that the biggest all-or-nothing risk is upstream in spec generation, not downstream in the remediation ladder.

2. Truthful progress for the initial spec-generation phase
I want a task that fixes the progress gap before pre-build starts.

This should address:
- truthful stage-level progress during the initial package-spec generation phase
- not just generic “Generating UiPath package...” keep-alives
- progress semantics for:
  - context loading
  - prompt assembly
  - LLM spec generation
  - parse / repair
  - spec validation / decomposition handoff
- ensure the user sees a continuous truthful progress story from initial package-spec generation into the later build pipeline

Important:
The current pipeline log panel is not enough if the initial phase still has only generic text.

3. Dependency version / assembly authority
I want a task that tightens dependency correctness for Studio-safe output.

This should address:
- replacing or reducing hardcoded dependency/version assumptions
- validating emitted versions against an approved source of truth
- not omitting packages that the generated XAML still references
- ensuring namespace imports / assembly references / detected activity usage stay aligned with `project.json`
- eliminating the current class of Studio restore drift

Important constraints
- do not propose a full rewrite unless justified
- do not lose the current downstream build/remediation pipeline unnecessarily
- do not assume the later pipeline stages compensate for the initial single-shot spec bottleneck
- keep the task definitions grounded in the current codebase, not just prior design ideas
- focus on package generation reliability and quality first

Please also include:
- your recommended task split
- which task should be highest priority
- what I should ask the implementation agents to tackle first
