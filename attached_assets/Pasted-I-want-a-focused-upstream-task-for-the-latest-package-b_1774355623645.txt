I want a focused upstream task for the latest package build failure:

[Dependency Resolution] FATAL: Package "UiPath.GSuite.Activities" is referenced by activities but has no validated version in the catalog, studio profile, or metadata service.

Context
The current generation correctly identified Google-related UiPath activities for the birthday greetings process, but the dependency authority layer does not recognize `UiPath.GSuite.Activities`, so the build aborts rather than inventing a version.

I want a task that fixes this properly and hardens against similar failures.

Please create the task in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Task requirements
1. Add validated package authority for `UiPath.GSuite.Activities`
- ensure it is covered by the appropriate generation metadata / validated package source
- ensure dependency resolution can emit a validated version/range for it

2. Audit for other common missing packages likely to recur
- review commonly generated UiPath packages relevant to our generation patterns
- identify a small high-value set of missing packages that should be added now
- avoid speculative bulk additions

3. Preserve strict dependency behavior
- do not fabricate dependency versions
- do not silently continue if a required package has no validated version authority
- the system should remain deterministic and Studio-safe

4. Improve diagnostics
- when a package is missing from validated authority, the failure should identify:
  - the missing package
  - which activity/workflow referenced it if available
  - which authority layer was missing it (catalog, studio profile, metadata service)

Important constraints
- do not implement dynamic “latest from NuGet” lookup as the primary fix unless strongly justified
- prefer deterministic, validated package authority
- keep this focused on fixing the missing package-authority gap and preventing nearby repeats
