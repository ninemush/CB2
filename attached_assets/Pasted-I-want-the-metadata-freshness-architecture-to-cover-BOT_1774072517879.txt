I want the metadata freshness architecture to cover BOTH:
1. package-generation metadata
2. UiPath integration endpoint metadata

Context:
We already identified that CannonBall should not rely on stale hardcoded values for:
- Studio compatibility
- activity/package versions
- package compatibility assumptions

I also want the same out-of-band freshness model applied to UiPath integration metadata:
- service endpoint roots
- API capability mappings
- endpoint confidence/verification status
- service deprecation/replacement notes

I do NOT want live metadata discovery in the package-generation critical path or other core runtime paths.

What I want:
Design a unified out-of-band metadata refresh architecture that keeps current:

A. Generation metadata
- UiPath Studio Compatibility Profile
- activity/package source-of-truth
- package version compatibility
- activity/property/enum compatibility

B. Integration metadata
- UiPath service endpoint map
- official vs inferred endpoint status
- API confidence levels
- service capability definitions
- deprecation / migration notes for service roots and APIs

Requirements:
1. Metadata must be refreshed automatically on a scheduled basis (daily is acceptable)
2. Runtime package generation must not perform live metadata discovery
3. Runtime integration logic should prefer local validated endpoint metadata rather than scattered hardcoded assumptions
4. The system must use local last-known-good snapshots at runtime
5. If refresh fails, the system continues operating on the last known-good snapshot
6. Freshness/staleness must be surfaced in logs and/or admin UI
7. Hardcoded stale fallbacks like 23.10.6 and guessed endpoint strings should be progressively replaced by snapshot-backed metadata

What I want you to define:
1. recommended overall architecture
2. the two metadata families and how they differ
3. the local snapshot file layout
4. what each file should contain
5. what official UiPath sources should feed the refresh job
6. how runtime generation should consume generation metadata
7. how runtime integration/probing should consume endpoint metadata
8. failure/staleness behavior
9. immediate next implementation tasks
10. acceptance criteria

Important constraints:
- Do NOT solve this with manual JSON updates as the primary method
- Do NOT add live-network latency to package generation
- Do NOT make runtime flows depend on external availability for metadata
- Keep generation deterministic and low-latency
- Keep integration endpoint handling current without scattering guessed strings across the codebase

Deliverable:
1. concise root-cause summary
2. unified metadata-refresh architecture
3. snapshot/schema proposal
4. runtime consumption model
5. failure/staleness behavior
6. phased implementation plan
7. acceptance criteria
