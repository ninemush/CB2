I want you to revise the UiPath-specific Run 3 architecture spec so it explicitly protects the core UiPath package generation path.

Context:
We are introducing a UiPath Platform Capability Discovery + UiPath Solution Mode Selection subsystem. This subsystem is essential because CannonBall uses the connected UiPath tenant’s available services to shape the TO-BE design, decide whether the solution should be UiPath RPA, UiPath Agent, or a Hybrid UiPath solution, and generate Future Enhancements recommendations in the PDD.

However, this new subsystem must NOT negatively impact the core UiPath package generation path.

What I need:
Revise the spec so it is clearly UiPath-specific and includes an explicit safeguard section that makes package generation resilient and independent from optional capability discovery failures.

Required framing:
Use these terms consistently:
- UiPath Platform Capability Discovery
- UiPath Solution Mode Selection
- UiPath RPA
- UiPath Agent
- Hybrid UiPath solution

Add a clearly labeled section:
Non-Negotiable Package Generation Safeguards

That section must include these architectural rules:
1. Capability discovery informs package generation and TO-BE design, but does not own or block package generation.
2. Standard UiPath package generation must remain functional on the minimal Orchestrator-backed UiPath RPA path.
3. Failures, uncertainty, or permission gaps in probing optional/advanced UiPath services must not fail package generation.
4. If the solution mode engine recommends UiPath Agent or Hybrid UiPath solution, but those capabilities are unavailable or uncertain, CannonBall must degrade safely to an implementable UiPath RPA path where possible, or explicitly mark the design as partially implementable without breaking the package builder.
5. The package builder must consume a normalized design contract, not raw probe logic.
6. No probe timeout, undocumented endpoint mismatch, or optional service discovery issue should directly produce package build failure.

The revised spec should still cover:
- problem statement
- target architecture
- UiPath-specific output contracts
- decision rules for UiPath RPA vs UiPath Agent vs Hybrid UiPath solution
- UiPath service confidence model
- PDD Future Enhancements contract
- implementation plan
- acceptance criteria

Important:
- Keep this UiPath-specific, not generic
- Treat the probe as essential
- Treat package generation stability as non-negotiable
- Do not let the new discovery/selection subsystem become a new regression source for the package builder

Deliverable:
Return the revised UiPath-specific architecture spec with the new safeguard section fully integrated.
