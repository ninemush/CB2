Please do a strict repo-alignment review of the two revised plans below.

Important:
Do not rewrite the plans from scratch.
Do not change the intent.
Do not reduce the scope.
I only want you to validate and revise them where they still refer to components, files, or architecture that may not actually exist in the current CannonBall codebase.

What I need from you:
1. Verify every referenced file, component, type, and function against the current repo.
2. If a referenced file/component does not exist, replace it with the actual current file/component responsible for that behavior.
3. If a referenced function name is wrong or outdated, replace it with the actual current implementation point.
4. Preserve the architecture and acceptance criteria.
5. Return the corrected plans in full.

Focus especially on validating these references:
- server/workflow-tree-assembler.ts
- server/workflow-spec-types.ts
- server/catalog/catalog-service.ts
- catalog/activity-catalog.json
- enrichWithAITree
- assembleWorkflowFromSpec
- /api/ideas/:id/download-package

If these do not exist exactly as named, replace them with the correct current CannonBall equivalents.

Also validate:
- whether the package download path is actually named that way
- whether the activity catalog is implemented through JSON/catalog service or through existing registry files
- whether the workflow spec assembly is actually in a tree assembler file or handled elsewhere today

Output format:
- Return the two plans in full, corrected and repo-aligned.
- Keep the structure unchanged.
- Do not give me commentary first.
- Just give me the corrected plans.

Here are the plans to validate and correct:

#112 - Pipeline Survivability Architecture
What & Why
Redesign the UiPath package generation pipeline around a progressive convergence model so it almost never ends in "no package generated." Today, three fail-closed paths still exist:

In full_implementation mode, if the quality gate fails after auto-remediation, buildNuGetPackage throws a QualityGateError (line 1693 of uipath-integration.ts) that kills the entire build with no fallback attempt.
If enrichWithAITree in uipath-integration.ts fails, times out, or returns a malformed WorkflowSpec, the pipeline can produce no usable XAML and end with an empty or missing package.
If buildNuGetPackage produces an empty buffer (e.g., ZIP archive creation fails), the pipeline declares FAILED with no recovery attempt.
This task eliminates all three paths by making every failure converge through the same canonical generateUiPathPackage entrypoint using automatic downgrade to baseline_openable, and by adding a new FALLBACK_READY status so the system can clearly distinguish degraded-success from clean-success or total failure.

All downgrade/retry paths must go through the existing canonical generateUiPathPackage function in uipath-pipeline.ts — no side entrypoints or parallel build paths.

Done looks like
When full_implementation fails quality gate remediation, the pipeline catches the QualityGateError, records a typed DowngradeEvent, and retries through the same generateUiPathPackage entrypoint with baseline_openable mode — no unhandled throw kills the build
DowngradeEvent is a dedicated typed field on PipelineResult (PipelineResult.downgrades: DowngradeEvent[]), separate from the generic warnings: PipelineWarning[] array. The warnings array may contain a human-readable summary of each downgrade for UI consumption, but the canonical structured data lives in downgrades. Fields: fromMode, toMode, reason, triggerStage, timestamp
A maxDowngradeAttempts guard (default: 1) prevents recursive downgrade loops — if baseline_openable also fails, the pipeline declares FAILED cleanly
When enrichWithAITree fails, times out, or returns unparseable output, the pipeline falls back to deterministic scaffold generation using approved process map nodes + approved SDD/PDD context when present, or map-only scaffold when document context is absent or weak
FALLBACK_READY is a first-class value in the PackageStatus type alongside READY, READY_WITH_WARNINGS, and FAILED — it is set whenever the pipeline converged through auto-downgrade or AI enrichment fallback
The pipeline cache distinguishes clean vs. degraded outcomes: the cache key includes a degradation component (e.g., whether the result was produced via downgrade or AI fallback) in addition to the existing ideaId:mode:mvMode key, so a FALLBACK_READY result is never served from cache when a clean READY generation is expected for the same input fingerprint. The CachedPipelineResult fingerprint comparison must also account for degradation state
The "structurally invalid or non-emittable archive" failure condition is broadened: if buildNuGetPackage produces an empty buffer, a corrupted ZIP, or an archive missing Main.xaml or project.json, the pipeline retries once with baseline_openable before declaring FAILED
A pre-assembly catalog property filter strips activity properties not present in the activity catalog from the WorkflowSpec tree before it reaches assembleWorkflowFromSpec, so non-catalog properties never produce quality gate violations
Any package produced via baseline_openable retry remains downloadable and deployable through the exact same downstream endpoints (/api/ideas/:id/download-package, deployment routes) with no special-case product path — the same archive format, the same NuGet structure, the same project.json schema
The front-end UiPathPackageCard in document-card.tsx handles the FALLBACK_READY status with distinct UI language: amber indicator, message like "Package generated with reduced scope — see Developer Handoff Guide for details", clearly distinguishable from READY_WITH_WARNINGS
Total package failure (FAILED) is reserved for truly catastrophic conditions: ZIP archive cannot be created, baseline_openable retry also fails, or no usable approved or inferred process structure exists (no map nodes and no SDD/PDD content to scaffold from)

Out of scope
Per-activity or per-sequence granular fallback within workflows (separate task)
Enhanced DHG reporting of auto-fix/stub/downgrade actions (separate task)
Changes to the UiPath deployment or download endpoints
Changes to quality gate rules or meta-validator correction categories
UI redesign beyond handling the new FALLBACK_READY status

Tasks
Define a typed DowngradeEvent interface with fields fromMode, toMode, reason, triggerStage, timestamp and add downgrades: DowngradeEvent[] as a dedicated field on PipelineResult, separate from the existing warnings: PipelineWarning[] array. Optionally emit a human-readable PipelineWarning summary for each downgrade, but the canonical structured data must live in downgrades.
Add FALLBACK_READY to the PackageStatus type in uipath-pipeline.ts and update the final status logic so it is set when any DowngradeEvent is present or the pipeline used AI enrichment fallback.
In uipath-integration.ts, wrap the QualityGateError throw at line 1693 with downgrade logic: catch the error, record a DowngradeEvent, and signal upward to the pipeline to retry with baseline_openable through the same generateUiPathPackage entrypoint. Add a maxDowngradeAttempts counter (default: 1) so the pipeline never enters a recursive retry loop. Ensure the retry uses the exact same downstream packaging and archive paths so the output is downloadable and deployable with no special-case handling.
In uipath-integration.ts, wrap the enrichWithAITree call (line 557) with a fallback path: if it fails, times out, or returns a non-success status, generate a deterministic workflow scaffold from the IdeaContext — use approved process map nodes + approved SDD/PDD content when present, or map-only scaffold when document context is absent or weak. This must produce a valid WorkflowSpec that can proceed through normal assembly via assembleWorkflowFromSpec.
Broaden the empty-buffer check in uipath-pipeline.ts (line 479) into a structural validity check: verify the buffer is non-empty, the archive is a valid ZIP, and it contains Main.xaml and project.json. If any check fails, record a DowngradeEvent and retry once with baseline_openable.
Add a pre-assembly catalog property filter: after the LLM produces the WorkflowSpec, iterate its activity nodes and strip any properties not present in the activity catalog (catalog/activity-catalog.json via catalog-service.ts) before passing to assembleWorkflowFromSpec. Log stripped properties as pipeline warnings.
Update the pipeline cache to distinguish clean vs. degraded outcomes: extend the cache key or fingerprint comparison to include degradation state (e.g., whether downgrades occurred or AI fallback was used), so a FALLBACK_READY result is never incorrectly served for a request that could produce a clean READY result.
Update the UiPathPackageCard in client/src/components/document-card.tsx to handle FALLBACK_READY with an amber status indicator and appropriate messaging that clearly distinguishes it from READY_WITH_WARNINGS.

Relevant files
server/uipath-pipeline.ts:48,131-176,277-336,340-780
server/uipath-integration.ts:37,63-70,555-572,1490-1710,2053-2056
server/ai-xaml-enricher.ts
server/xaml-generator.ts:51-80,3490-3510
server/workflow-tree-assembler.ts
server/workflow-spec-types.ts
server/catalog/catalog-service.ts
catalog/activity-catalog.json
server/uipath-quality-gate.ts:1711-1717
client/src/components/document-card.tsx:462-478

#113 - Granular Fallback and DHG Reporting
What & Why
Today, when a workflow has invalid activities, the entire workflow is replaced with a stub via generateStubWorkflow. This discards all valid logic — variables, arguments, control flow, and surrounding sequences — in the workflow. The remediation path in uipath-integration.ts (lines 1604-1672) operates at whole-workflow granularity only.

Additionally, the auto-fix actions (autoFixSummary), stub insertions (earlyStubFallbacks), quality issues (collectedQualityIssues), and downgrade decisions made during the pipeline are logged to console but not systematically fed into the Developer Handoff Guide. The DHG currently accepts only stubbedWorkflows?: string[] and qualityIssues?: DhgQualityIssue[] — it has no structured input for remediation actions, their granularity, or the original intent of replaced components.

This task adds per-activity and per-sequence fallback granularity and introduces a canonical PipelineOutcomeReport object that flows from the build engine into DHG generation as the single source of truth.

Done looks like
When a specific activity within a workflow fails validation (as identified by ClassifiedIssue from classifyQualityIssues — not ad hoc string matching), only that activity is replaced with a ui:Comment stub that preserves the original activity type, DisplayName, and the blocking reason — the surrounding sequence structure, workflow arguments, variables, and control-flow shape (TryCatch, If, ForEach) remain intact
Per-activity and per-sequence fallback must preserve workflow signatures and invoke compatibility: all InArgument, OutArgument, and InOutArgument declarations on the workflow remain intact, all variables declared in parent scopes are preserved, and any InvokeWorkflowFile contracts that reference the workflow from other XAML files continue to work without modification
When multiple activities within a single <Sequence> block are all invalid but the sequence boundaries and parent control flow are valid, the sequence content is replaced with a stub sequence that documents the intended behavior — the parent workflow structure, arguments, and variables are preserved
Whole-workflow stub replacement via generateStubWorkflow only happens as a last resort when per-activity and per-sequence repair still cannot produce valid XAML (verified by re-running runQualityGate after each repair level)
The escalation ladder is explicit: per-activity → per-sequence → per-workflow, with re-validation at each level before escalating
A new PipelineOutcomeReport interface captures all remediation actions as typed, structured entries. Each remediation entry includes: level: "activity" | "sequence" | "workflow", file: string, remediationCode: string (stable machine-readable code, e.g., "STUB_ACTIVITY_CATALOG_VIOLATION", "STUB_SEQUENCE_WELLFORMEDNESS", "STUB_WORKFLOW_BLOCKING"), originalTag?: string, originalDisplayName?: string, reason: string, classifiedCheck: string, developerAction: string, estimatedEffortMinutes: number
PipelineOutcomeReport also includes sections for auto-repairs (from autoFixSummary, each with a stable repairCode), downgrade events (from DowngradeEvent[] introduced in the survivability task), and quality gate warnings — replacing the current practice of passing loose arrays
DhgOptions in xaml-generator.ts is extended to accept outcomeReport?: PipelineOutcomeReport instead of the current separate stubbedWorkflows, qualityIssues, and generationModeReason fields (the old fields remain supported for backward compatibility)
The DHG output includes new sections organized by remediation category:
Fully Generated — workflows that passed quality gate without intervention
Auto-Repaired — specific fixes applied (what was fixed, in which file, how), each with its repairCode
Stubbed — items replaced at activity, sequence, or workflow level, with original intent, blocking reason, remediationCode, and developer action needed
Downgraded — mode changes from full to baseline, with trigger reason (from DowngradeEvent)
Each stubbed item in the DHG includes a concrete developer action item with estimated effort (e.g., "Recapture UI selector for Click activity in ProcessInvoice.xaml — estimated 15 min")
The front-end UiPath package card shows a remediation summary count when the package is READY_WITH_WARNINGS or FALLBACK_READY (e.g., "3 activities stubbed, 2 auto-repaired — see DHG")
Invalid activity detection in the remediation path uses ClassifiedIssue.check and ClassifiedIssue.file from classifyQualityIssues to locate the failing activity tag within the XAML, rather than ad hoc regex or string matching against violation detail text
XAML replacement for per-activity and per-sequence stubbing should prefer structural or parser-assisted replacement (using the existing fast-xml-parser dependency for tag-level operations) where possible, rather than relying purely on regex/string substitution — especially when identifying activity boundaries, sequence boundaries, and nested element scope

Out of scope
Changes to the pipeline convergence model, PackageStatus types, or downgrade retry logic (covered by the survivability task)
Changes to the quality gate rules themselves or adding new violation checks
Changes to the meta-validator correction categories
Changes to deployment or download endpoints

Tasks
Define the PipelineOutcomeReport interface with typed entries for stubs (with level, file, remediationCode, originalTag, originalDisplayName, reason, classifiedCheck, developerAction, estimatedEffortMinutes), auto-repairs (from autoFixSummary entries, each with a stable repairCode such as "REPAIR_ALIAS_NORMALIZE", "REPAIR_ENUM_CORRECTION", etc.), downgrade events (from DowngradeEvent[]), and quality gate warnings. The remediationCode and repairCode fields must be stable machine-readable codes, not free-text.
Implement per-activity stub replacement: in the quality gate remediation path in uipath-integration.ts, when classifyQualityIssues identifies a blocking issue tied to a specific activity (via ClassifiedIssue.file and ClassifiedIssue.check), locate that activity tag in the XAML using parser-assisted or structural matching (preferring fast-xml-parser for tag boundary detection over pure regex), and replace it with a ui:Comment stub documenting the original activity type, DisplayName, and reason. Preserve the surrounding sequence structure, all workflow arguments (InArgument, OutArgument, InOutArgument), variables, and control-flow elements so the workflow signature and invoke compatibility remain intact.
Implement per-sequence stub replacement: when multiple activities within a single <Sequence> block are invalid but the parent structure is valid, replace the sequence children with a single stub Comment while preserving the <Sequence> element, its DisplayName, and all parent-scoped variables and arguments.
Update the escalation ladder in uipath-integration.ts (around lines 1596-1672): try per-activity replacement first, re-run runQualityGate to verify, escalate to per-sequence if still failing, re-run quality gate again, and only then fall back to per-workflow generateStubWorkflow. Record each remediation action as a PipelineOutcomeReport entry with the appropriate remediationCode.
Wire PipelineOutcomeReport through the build pipeline: populate it from autoFixSummary, earlyStubFallbacks, collectedQualityIssues, and any DowngradeEvent[] from the survivability task, and pass it through buildDhgFromBuildResult into generateDeveloperHandoffGuide.
Extend DhgOptions in xaml-generator.ts to accept outcomeReport?: PipelineOutcomeReport and add DHG sections for each remediation category (Fully Generated, Auto-Repaired with repairCode, Stubbed by level with remediationCode, Downgraded) with developer action items and estimated effort for each stubbed item.
Update the UiPathPackageCard in document-card.tsx to show a remediation summary line when the package has warnings or fallback status (e.g., "3 activities stubbed, 2 auto-repaired").

Relevant files
server/uipath-integration.ts:37-46,63-70,728-776,1185-1186,1490-1710
server/xaml-generator.ts:3490-3510,3512-3565,3620-3625,4783-4850
server/uipath-pipeline.ts:131-158,277-324,680-735
server/uipath-quality-gate.ts:1711-1730
server/workflow-analyzer.ts
server/workflow-tree-assembler.ts
server/workflow-spec-types.ts
client/src/components/document-card.tsx:462-478
Dependencies
Pipeline survivability architecture — auto-downgrade, AI fallback, FALLBACK_READY status, catalog enforcement
