Please refine both tasks based on this direction:

Overall intent:
- these should be updates/additions to the current system
- not complete rewrites
- preserve existing good mechanisms and extend them where the current repo has real gaps

Task #403 refinement guidance

The current decomposer prompt is already fairly rich:
- compact catalog summary is already injected
- typed property rules already exist
- enum guidance already exists
- InvokeWorkflowFile guidance already exists
- output-shape constraints already exist

So do NOT frame #403 as if prompts currently have no rules.
Instead, refine it as:
- tightening and grounding the existing prompt boundary
- adding missing hard constraints
- injecting missing Studio profile context
- adding a bounded iterative LLM correction loop only where deterministic patching is weak

Please update #403 accordingly:

1. Reframe the prompt work
Say:
- strengthen the existing decomposer and legacy prompt paths with explicit boundary language
Not:
- add prompt constraints from scratch

2. Add explicit hard constraint language
For the decomposer prompt and legacy UIPATH_PROMPT, add:
- ONLY use activities present in the injected catalog/template summary
- ONLY use listed properties for each activity
- obey listed enum values exactly
- declare all referenced variables
- do not invent activity names, property names, or dependency packages
- follow catalog/template syntax rules such as attribute vs child-element usage

3. Inject Studio profile context
Use metadataService.getStudioProfile() and inject:
- Studio line/version
- target framework
- expression language
- minimum required packages if relevant
This should be added to both the decomposer and legacy prompt paths.

4. Keep deterministic correction, add iterative LLM correction selectively
Do NOT replace the current deterministic/string-based correction approach wholesale.
Instead:
- keep safe deterministic corrections as first-pass repair
- add a bounded iterative LLM regeneration step only for categories where patching is fragile:
  - unknown activity
  - invalid activity property
  - undeclared variable
  - expression syntax
  - property syntax / wrapper issues
Max 1-2 correction rounds.

5. Be explicit about the correction flow
Refine the task so it says:
- initial generation
- existing quality gate / meta-validation review
- if fixable fragile categories remain, feed the original workflow spec plus specific findings back to the generating LLM for targeted workflow regeneration
- re-run validation after each correction round
- stop after 1-2 rounds or when the workflow is clean enough

6. Token budget check remains required
Keep token budget verification as a real acceptance item, since catalog/template guidance is already sizable.

Task #405 refinement guidance

The repo already has substantial run-detail backend plumbing:
- idea-scoped run endpoints already exist
- progress/stream endpoints already exist
- outcomeReport and phaseProgress are already parsed in current routes

So do NOT frame #405 as building debug data access from scratch.
Instead, refine it as:
- adding an admin-wide run history/list capability
- exposing admin-safe detail access cleanly
- building the admin Debug tab UI on top of mostly existing data

Please update #405 accordingly:

1. Reframe the backend scope
Say:
- add admin-wide run history endpoint with pagination/filtering
- add admin detail endpoint or reuse the existing run-detail response shape for admin debug access
- expose dhgContent/stageLog consistently where needed
Not:
- create all debug backend APIs from nothing

2. Add storage requirements explicitly
The current storage layer has getGenerationRun and getLatestGenerationRunForIdea but not a paginated “list all runs” method.
Please add storage/repository work for:
- list generation runs with pagination
- optional filtering by status / idea / date
- counts/summary fields for warnings/remediations when practical

3. Reuse existing SSE/progress infrastructure
Do not invent new live-streaming infrastructure.
Reuse the existing run progress / stream endpoints for active runs where possible.

4. Keep the admin UI scope
The Debug tab should still include:
- run history table
- run detail panel/modal
- stage timeline
- quality warning breakdown
- remediations
- auto-repairs
- degradations
- emission gate violations
- rendered DHG
- raw outcome_report JSON
But describe it as a UI over existing run data plus the new admin-wide listing capability.

5. Maintain admin-only access
Keep consistent with existing admin-only tabs and APIs.

Deliverable requested
Please return revised versions of:
- #403
- #405

Each revised task should:
- preserve current working mechanisms
- clearly identify what is additive vs replacement
- avoid implying a full rewrite where the repo already has partial capability
