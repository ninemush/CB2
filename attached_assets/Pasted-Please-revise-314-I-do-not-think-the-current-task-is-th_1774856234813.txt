Please revise #314. I do not think the current task is the complete upstream fix for the issues validated in BirthdayGreetingsV7.

The current draft correctly identifies some real problems:
- reachability checker false positives
- quality gate false passes
- XAML attribute quote/escaping gaps

However, it is incomplete because it focuses too much on downstream detection and not enough on the upstream generation defects still visible in the actual package artifacts.

What has been directly validated from the V7 artifacts
1. Reachability checker likely has false positives
- Main.xaml clearly contains InvokeWorkflowFile references to:
  - InitAllSettings.xaml
  - Dispatcher.xaml
  - Performer.xaml
  - ContactResolver.xaml
  - GenerateAndValidateMessage.xaml
  - SendGreetingOrNotify.xaml
- so the pipeline warning that 5 workflows are unreachable from Main.xaml appears incorrect for this package

2. InitAllSettings.xaml is not actually Studio-openable
- Studio reports DynamicActivity / Implementation is null
- the file still contains the same upstream type-generation defect we have seen before:
  - ForEach x:TypeArguments="scg2:DataRow"
  - but ForEach.Values is typed as InArgument(x:String) and bound to dt_Settings.Rows / dt_Constants.Rows
- this is not just a quality-gate detection problem; it is an upstream generation/type-correctness bug

3. ContactResolver.xaml is not truly fully generated
- the file still contains a Stub Warning block and Log Completion only after the variable declarations
- Studio also reports the System.Object literal problem
- the file is being classified more optimistically than it should be
- this is both:
  - an upstream type-emission problem
  - and a truthfulness / classification problem

4. The stripping/downgrade cascade is still active
- the pipeline still stripped 26 non-catalog properties across 6 workflows
- 3 activities had 3+ properties stripped
- auto-downgrade to baseline_openable still happened
- this remains a major upstream degradation mechanism in V7 and should not be omitted from the tasking

What I want changed
Please revise #314 so it covers five issue areas, not just three:

1. Fix reachability checker false positives
- keep this part
- ensure graph analysis uses finalized assembled XAML and correct path normalization
- confirm whether reachability should contribute to downgrade severity

2. Fix quality gate false passes
- workflows that fail to load in Studio must not be marked Studio-openable / Fully Generated
- workflows containing stub-warning/fallback content must not be classified as fully generated

3. Fix InitAllSettings upstream type-generation correctness
- explicitly include the ForEach.Values / TypeArguments mismatch
- if iterating DataTable.Rows, the values/type wiring must be emitted correctly upstream
- do not treat this as only a downstream validation problem

4. Fix Object literal / variable type emission correctness
- explicitly include the rule that x:Object variables/arguments must not be emitted with literal defaults such as False where UiPath does not allow that
- this should be fixed at generation time, not only caught later by the quality gate

5. Include the still-active stripping/downgrade cascade
- the task must acknowledge that excessive non-catalog property stripping and the auto-downgrade to baseline_openable are still major contributors to package degradation in V7
- even if quote escaping is one specific cause for one workflow, the broader stripping/downgrade pattern remains in scope

What I want back
Please return a revised task in the usual format with:
- title / objective
- root-cause summary
- done looks like
- out of scope
- tasks
- relevant files
- downstream impact review

Important:
- do not position this as only a quality-gate/reporting cleanup task
- keep the focus on upstream correctness plus downstream truthfulness
- make sure the revised task explicitly addresses:
  - InitAllSettings ForEach typing
  - ContactResolver Object-literal/type emission
  - false “Fully Generated” / “Studio-openable” classification
  - stripping/downgrade as an active root-cause class in V7
