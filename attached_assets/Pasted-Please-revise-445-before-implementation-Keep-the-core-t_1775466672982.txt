Please revise #445 before implementation.

Keep the core task exactly focused on InvokeWorkflowFile.Arguments dotted child-element prefix consistency.

Make these changes:

1. Remove this Additional Requirements item entirely:
"If the prefix fix involves generating or correcting any variable references (e.g., argument bindings inside InvokeWorkflowFile.Arguments), any fallback variable creation for unresolved Assign.To targets must be deterministic, declared in the appropriate workflow scope, and recorded as a handoff remediation with provenance. In debug/test mode, fail if a fallback variable is emitted without a matching declaration in the same workflow."

Reason:
That requirement belongs to #444 ValueIntent Serialization Completeness, not #445. It broadens scope unnecessarily and mixes two separate failure classes.

2. Keep and strengthen the idempotence/generic dotted-tag requirements. The final Additional Requirements section for #445 should focus only on prefix/dotted-tag behavior.

Replace the Additional Requirements section with this exact text:

Additional Requirements
- The prefix-consistency correction must be idempotent and generic for all dotted child-element tags (<prefix:Activity.Property>), not just InvokeWorkflowFile.Arguments.
- A second compliance/normalization pass over already-correct XAML must produce byte-identical dotted child-element tags and must not introduce or remove prefixes.
- If the fix changes the timing or order of prefix correction relative to Input/Output expansion, add a regression test proving that both pre-existing dotted tags and newly generated dotted tags from expansion receive the same final prefix treatment.

3. Keep the rest of #445 the same, including:
- identifying the exact code path
- fixing dotted child-element prefix consistency after all compliance transforms
- adding post-compliance validation/correction before XML well-formedness check
- regression tests for InvokeWorkflowFile with Input and Output arguments

4. Ensure the regression tests include:
- specific InvokeWorkflowFile.Arguments mismatch case
- idempotency on already-correct dotted tags
- mixed scenario with pre-existing dotted tags plus Input/Output-generated dotted tags

Output the corrected final version of #445 only.