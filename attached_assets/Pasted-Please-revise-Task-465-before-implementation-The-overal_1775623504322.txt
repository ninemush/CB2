Please revise Task #465 before implementation.

The overall direction is correct, but the current version underestimates the defect mix and groups together required-property classes that need different repair strategies.

Required changes:

1. Explicitly include Assign.To in scope
Add Assign.To to the benchmark-hit repeated defect classes addressed by this task.
However, do not treat Assign.To as a normal fallback-eligible property.
State explicitly that Assign.To is a target-reference property and may require:
- valid upstream target selection
- or variable declaration/repair
rather than literal fallback emission.

2. Split the task into two repair subfamilies
Add an explicit distinction between:
- fallback-eligible required properties
- target-reference required properties

Fallback-eligible examples:
- LogMessage.Message
- If.Condition / While.Condition when a safe structural fallback policy is justified
- entity-type properties where a safe contract-valid fallback exists

Target-reference examples:
- Assign.To

The task must not apply one generic fallback policy across both subfamilies.

3. Acknowledge upstream expression-lowering dependency
Add an explicit rule:
- If a required property appears unresolved because it still contains a leaked structured JSON expression/object, the pipeline must attempt expression lowering before fallback policy is applied.
- Fallback policy is not the first response to raw JSON-shaped values.

If the lowering path is already handled elsewhere, say that this task must integrate with it rather than duplicate it.

4. Tighten the success criteria
Replace vague "material reduction" language with a concrete benchmark target such as:
- unresolved required-property defects drop by at least 30% across both benchmark families
- repeated benchmark-hit classes addressed by this task show clear before/after reduction
You may choose a different exact threshold if a better evidence-based target exists, but it must be concrete.

5. Keep the task general, not use-case-specific
Do not narrow the task to a single benchmark workflow.
Keep it general to repeated benchmark-hit classes across Birthday and Coupa, but bounded to shared activity/property code paths rather than workflow-specific hacks.

6. Preserve strict validation and explicit blocking
If no valid source, no safe fallback, and no target-reference repair path exists, the pipeline must still block explicitly rather than emit a structurally incomplete or semantically fake value.

Output the corrected final task only.