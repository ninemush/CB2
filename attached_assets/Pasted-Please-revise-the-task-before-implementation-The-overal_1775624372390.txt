Please revise the task before implementation.

The overall direction is good, but the current version still mixes two different repair systems and remains too brittle.

Required changes:

1. Remove target-reference properties from this task
Do not include Assign.To or other target-reference properties in this implementation task.
Those require source-target reconstruction and possibly variable declaration/scope repair, which is a separate authority layer from required-property fallback/value selection.

This task should be limited to fallback-eligible required-property classes only, such as:
- LogMessage.Message
- If.Condition / While.Condition
- entity-type properties confirmed by evidence
- mail Subject/Body properties confirmed by evidence

2. Add an explicit follow-up note
State that target-reference properties like Assign.To are intentionally excluded and should be handled by a separate follow-up task focused on variable/target reconstruction rather than fallback policy.

3. Soften the success metric
Keep a concrete benchmark expectation, but do not make the task succeed or fail solely on a global ≥30% reduction threshold.
Instead require:
- clear before/after reduction for the included repeated classes
- evidence that the shared decision path changed the included classes in the intended direction
A quantitative target may remain as a secondary measure, not the sole acceptance criterion.

4. Clarify the implementation center
Do not require every fix to be forced into required-property-enforcer.ts if the evidence shows the shared failure point is earlier.
Instead state:
- implement the shared decision layer at the earliest common path supported by the evidence
- required-property-enforcer.ts remains the default primary target unless the evidence proves an earlier shared path is the real source of failure

5. Replace hard-coded attached artifact filenames
Do not hard-code one-off ZIP filenames in the task text.
Refer instead to:
- the latest confirmed Birthday and Coupa verification bundles
- or the evidence documents derived from them

Do not broaden scope otherwise.
Output the corrected final task only.