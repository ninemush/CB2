Please revise the task before implementation with the following adjustments:

1. Clarify the pre-emission contract map data source.
State explicitly that the contract map used by the tree assembler must be built from the workflow spec argument arrays available before assembly, not by reusing buildWorkflowContracts from workflow-contract-integrity.ts. The existing buildWorkflowContracts remains the post-emission auditor only.

2. Define unresolved callee behavior explicitly.
Add a design rule:
- If an InvokeWorkflowFile targets a workflow that has no pre-emission spec-backed contract available, do not invent a contract.
- In that case, fall through to the current emission behavior and let the existing post-emission validator/canonicalizer act as safety nets.
- Record a diagnostic that the callee contract was unavailable pre-emission.

3. Tighten argument-name matching rules.
Add a rule that caller binding names should be matched against callee contract names using:
- case-insensitive comparison
- optional stripping of in_/out_/io_ prefixes on the caller side before comparison
If no supported match exists after these normalizations, emit an unsupported binding diagnostic rather than guessing.

4. Split file references into two groups:
- Primary files to touch
- Reference files / safety-net files expected to remain unchanged
Primary files should stay narrow and match the actual intended blast radius.
If workflow-contract-integrity.ts, invoke-binding-canonicalizer.ts, xaml-generator.ts, or final-artifact-validation.ts are included only for reference, label them that way rather than as primary modification targets.

5. Specify the test location.
Direct regression coverage to the existing test home at:
- server/__tests__/pipeline-regression.test.ts
unless there is a strong reason to extend an existing more specific test file instead.

6. Optional but useful:
Add one benchmark success measure:
- post-emission invoke-binding canonicalizer repair count should drop materially on benchmark runs after the pre-emission contract fix
You do not need to hard-code an exact percentage if that would be too brittle, but the task should state that this count is expected to fall significantly.

Do not broaden scope otherwise.
Output the corrected final task only.