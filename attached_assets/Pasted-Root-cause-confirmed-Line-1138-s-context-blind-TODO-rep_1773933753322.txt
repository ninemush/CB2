Root cause confirmed. Line 1138's 
context-blind TODO_ replacement strips 
tokens from XML tag names, producing 
< DisplayName="..."> which is invalid.

Fix line 1138 to skip matches that 
appear immediately after < or </ 
(i.e. inside tag names).

Replace the current:
  .replace(/TODO_\w*/g, '')

With a context-aware replacement that 
only targets TODO_ tokens in attribute 
values and text content, never in 
tag names:

  .replace(
    /(<\/?)\s*TODO_\w*|TODO_\w*/g,
    (match, tagStart) => {
      // If preceded by < or </ 
      // this is a tag name — 
      // preserve the bracket and 
      // replace the whole tag with 
      // a comment placeholder
      if (tagStart) {
        return `${tagStart}ui:Comment 
          Text="REVIEW: Unknown activity 
          type was generated here — 
          implement manually"`
      }
      // Otherwise safe to remove
      return ''
    }
  )

Wait — actually this approach is still 
fragile because converting a self-closing 
tag to a Comment mid-replacement is 
complex. 

Better approach: do the replacement 
in two passes using the already-parsed 
XAML structure rather than raw string 
replacement.

Since fast-xml-parser is already imported 
and used in uipath-integration.ts, use 
it to identify and handle TODO_ tokens 
correctly:

Pass 1 (before raw string replacement):
  Scan the XAML string for any opening 
  tag whose element name contains 
  TODO_ or PLACEHOLDER_ using this 
  targeted regex:
  
  /<(ui:)?(?:TODO_|PLACEHOLDER_)\w*/g
  
  For each match, replace the entire 
  tag (from < to the closing > or />) 
  with:
  <ui:Comment Text="REVIEW: Unknown 
  activity type — implement manually" />
  
  This handles the tag-name case 
  explicitly before the attribute-value 
  replacement runs.

Pass 2 (existing replacement, now safe):
  .replace(/TODO_\w*/g, '')
  .replace(/PLACEHOLDER_\w*/g, '')
  
  After Pass 1 has handled all tag-name 
  occurrences, Pass 2 safely strips any 
  remaining TODO_ tokens from attribute 
  values and text content without risk 
  of corrupting tag names.

Also elevate the debug log at 
xaml-generator.ts line 4705 from 
console.debug to console.warn so the 
full XAML content is captured in the 
log system when validation fails. This 
ensures future failures are diagnosable 
without log rollover issues.

COMPLETION CRITERIA

1. Generate WeatherAndTravelNotification 
   — zero XML parse errors

2. If any activity type contained a 
   TODO_ token, confirm it appears as 
   a ui:Comment in the generated XAML 
   rather than as a malformed tag

3. Confirm "Invalid space after '<'" 
   error does not recur

4. All existing tests still pass

5. New test: XAML containing 
   <ui:TODO_HttpClient .../> is 
   converted to a ui:Comment element, 
   not to <ui: .../> or < .../>

6. Elevate line 4705 log from 
   console.debug to console.warn