The UiPath generation race condition is not fully fixed yet. Please investigate and fix the remaining trigger handoff bug.

Observed runtime behavior:
- triggerUiPathGen is received from the server
- pendingUiPathGen is set correctly
- done handler preserves "Generating UiPath package..." correctly
- the effect fires once and logs:
  effect deferred — isStreaming=false, isGeneratingDoc=true, inFlight=false
- finally block calls stopDocStreaming({ force: true })
- after that, nothing else happens
- no second effect execution is observed
- POST /api/ideas/:ideaId/generate-uipath is never sent

Interpretation:
The pending trigger is preserved, but the effect is not reliably re-evaluating after isGeneratingDoc is cleared. This means the trigger handoff is still level-triggered in a way that can get stuck under React batching.

What I want:
Make the UiPath generation trigger fully edge-safe and batching-safe so it cannot get stuck after a deferred evaluation.

Recommended direction:
1. Prefer replacing the boolean pending trigger with an explicit trigger token / request id / incrementing counter.
   - Example concept:
     - pendingUiPathGenRequestId
     - lastConsumedUiPathGenRequestId
   - When triggerUiPathGen arrives, increment the request id
   - The effect watches the request id and blocking state
   - Once isStreaming=false and isGeneratingDoc=false and no generation is in flight, consume the unhandled request id and start generation
   - This avoids the “already true” boolean problem entirely

2. Also review ordering:
   - ensure stopDocStreaming({ force: true }) or equivalent blocking-state cleanup happens before the generation handoff is considered complete
   - do not rely on a stale render or a one-time effect pass

3. Keep idempotency:
   - no duplicate generate-uipath requests
   - if a request is already in flight, a newly received trigger should not start another concurrent generation

4. Preserve the existing improvements:
   - keep intent-specific progress text
   - keep instrumentation
   - keep duplicate-request prevention
   - keep cleanup on idea change

Requirements:
- Do not patch this by simply increasing delays
- Do not rely on another fragile timeout
- The trigger must survive React batching
- The trigger must be consumed only after the actual fetch('/api/ideas/:ideaId/generate-uipath') starts
- Add logs showing:
  - trigger request id created
  - trigger deferred due to blocking state
  - trigger re-evaluated after blocking state clears
  - trigger consumed
  - fetch started

Please:
1. diagnose the exact remaining cause
2. implement the robust fix
3. explain why the trigger can no longer get stuck after a deferred effect pass
4. tell me exactly what I should test immediately afterward
