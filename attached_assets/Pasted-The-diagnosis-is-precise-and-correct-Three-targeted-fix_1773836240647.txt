The diagnosis is precise and correct. Three targeted fixes only.
Do not change anything else.

FIX 1 — Remove unknown-activity and undeclared-variable from 
BLOCKING_CHECKS

In server/uipath-quality-gate.ts, move these two checks from 
BLOCKING_CHECKS to WARNING_CHECKS:
  - "unknown-activity"
  - "undeclared-variable"

Also fix the default-to-blocking catch-all at line 1764:
  Current:  isBlocking = severity === "error" && 
              (BLOCKING_CHECKS.has(check) || !WARNING_CHECKS.has(check))
  Change to: isBlocking = severity === "error" && 
              BLOCKING_CHECKS.has(check)

The catch-all default-to-blocking is the root of the over-stubbing 
problem. Any check not explicitly listed in BLOCKING_CHECKS must 
default to WARNING, not blocking. The blocking list must be an 
explicit allowlist, not a catch-all.

After this change, the only checks that trigger STUB_BLOCKING_FALLBACK 
are:
  xml-wellformedness, object-object, pseudo-xaml, fake-trycatch,
  project-json-parse, main-xaml, archive-manifest-parity, 
  archive-content-parity

Everything else is a warning. This is the correct policy.

FIX 2 — Auto-declare undeclared variables instead of flagging them

In the makeUiPathCompliant pass or the catalog auto-correction layer, 
when a variable reference is found in a bracketed expression that is 
not declared in the enclosing Sequence.Variables, auto-declare it:

  1. Find the nearest enclosing <Sequence> element
  2. Find or create its <Sequence.Variables> child
  3. Infer type from naming convention:
       out_* or str_* → x:String
       int_* or i_* → x:Int32  
       bool_* or b_* → x:Boolean
       dt_* → s:DataTable
       Everything else → x:Object
  4. Add: <Variable x:TypeArguments="[type]" Name="[varName]" />
  5. Log: "Auto-declared variable [name] as [type] in [workflow]"

This runs BEFORE the quality gate so the undeclared-variable check 
never fires on auto-correctable variables.

FIX 3 — Add ui:Delay to the activity registry AND bootstrap catalog

In server/uipath-activity-registry.ts add ui:Delay to the known 
activities list.

In catalog/activity-catalog.json add to UiPath.System.Activities:
{
  "className": "UiPath.Core.Activities.Delay",
  "displayName": "Delay",
  "browsable": true,
  "properties": {
    "Duration": {
      "direction": "In",
      "required": true,
      "xamlSyntax": "attribute",
      "argumentWrapper": null,
      "clrType": "System.TimeSpan"
    }
  }
}

VERIFICATION — do this before reporting back

After applying all three fixes, run a generation and check the 
logs for:
  1. No STUB_BLOCKING_FALLBACK in any workflow
  2. "Auto-declared variable" log entries confirming FIX 2 is 
     running
  3. ui:Delay appearing as a real activity in generated XAML, 
     not stripped or commented

Then separately investigate the missing workflow files issue. 
Show me the AI decomposition output for WeatherAndTravelNotification 
— specifically how many sub-workflows the enrichment stage produced 
and what their names were. Do not fix it yet, just show me the data. 
We will address it after the stub policy is fixed and verified.

All 113 tests must still pass. Send the package.