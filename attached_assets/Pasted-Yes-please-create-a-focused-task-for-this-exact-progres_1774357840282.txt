Yes — please create a focused task for this exact progress gap.

Context
The latest run shows that package spec decomposition is happening:
- scaffold generation completed
- 10 workflows were discovered
- per-workflow detail generation is now running sequentially

But the user-facing UI still shows only:
- “AI Generation”
- “Generating UiPath package...”

for hundreds of seconds.

This means the decomposed spec-generation phase is still not surfacing truthful sub-step progress to the user.

I want a focused task that addresses this specific issue.

Please create the task in the usual format:
- title / objective
- root-cause summary
- impact analysis
- done looks like
- out of scope
- tasks
- relevant files
- acceptance criteria
- dependencies

Task requirements

1. Per-workflow spec-decomposition progress events
- emit progress events for:
  - context loading
  - prompt assembly
  - scaffold generation start/complete
  - each workflow detail generation start
  - each workflow retry
  - workflow stub/failure after retry exhaustion
  - merge
  - schema validation
  - handoff to build pipeline

2. Observer-run propagation
- verify these spec-decomposition progress events propagate correctly through the observer run path
- ensure the frontend subscribed to the observer stream actually receives the per-workflow events

3. Active-step visibility in UI
- the progress UI should show:
  - current workflow name
  - current workflow index / total (e.g. 3 of 10)
  - active elapsed time for the currently running step
  - retry/stub warning when relevant
- do not rely on a generic “AI Generation” label when real sub-step data exists

4. Truthful sequencing
- the progress story must remain continuous from:
  - scaffold generation
  - through workflow detail generation
  - into merge/validation
  - and then into the existing build pipeline stages

Important constraints
- do not treat this as a generic UI redesign task
- focus on truthful progress propagation for decomposed spec generation
- keep any percentage/progress math truthful; if percent complete is not reliable, do not invent it
- use “workflow X of N” and active elapsed time as the primary progress signal if that is the most accurate model

Please also include:
- whether the root problem is event emission, observer forwarding, frontend rendering, or some combination
- what I should test immediately in a live run
