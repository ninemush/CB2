Yes — this confirms the real upstream problem is code duplication around chat-history-to-LLM preparation.

We cannot keep separate implementations of this logic.
There must be exactly one canonical function that prepares persisted chat history for LLM calls, regardless of which event or route triggered the generation.

Current problem:
There are three separate paths preparing chat history for LLM calls:
1. Chat path
   - correctly filters to user/assistant roles
2. Document generation path
   - broken: allows system-role messages through
3. Document revision path
   - broken: raw history map with unsafe role cast

This is not acceptable.
The same normalization/validation logic must not be reimplemented differently in multiple places.

What I want:
Create an upstream refactor so all chat-history-to-LLM preparation flows through one shared canonical function.

Required design:
1. Create one reusable helper for preparing chat history for LLM calls
   - input: persisted chat history
   - output: normalized LLM-safe messages
   - only user/assistant roles allowed in the final messages array
   - filter internal/system-only content as needed
   - preserve any existing marker filtering rules in a configurable or shared way
   - include runtime validation so invalid roles cannot survive

2. Replace all duplicate call-site logic with this helper
   - chat path
   - document generation path
   - document revision path
   - any other path that currently maps history directly into LLM messages

3. Remove unsafe casts
   - no more:
     - m.role as "user" | "assistant"
   - normalization must be explicit and runtime-safe

4. Make the helper provider-aware enough for Anthropic message constraints
   - final messages array must be valid for Anthropic by construction
   - system content must never appear as a message entry in Anthropic calls

Deliverable:
1. concise root-cause summary
2. upstream implementation task with:
   - objective
   - done looks like
   - out of scope
   - tasks
   - relevant files
3. acceptance criteria that require:
   - exactly one canonical chat-history-to-LLM preparation function for these paths
   - document generation no longer sends system-role messages
   - document revision no longer sends system-role messages
   - unsafe role casts are removed from affected call sites
   - Anthropic message arrays are valid by construction

Important:
- This is an upstream refactor + contract fix
- Do not patch only one route
- We want one shared function used across all triggering paths
